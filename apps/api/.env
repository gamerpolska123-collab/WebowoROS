# API — NestJS Backend
NODE_ENV=development
PORT=4000

# Database (PostgreSQL via Docker service name)
DATABASE_URL=postgresql://ros_user:ros_password@postgres:5432/restaurant_db

# Cache (Redis via Docker service name)
REDIS_URL=redis://redis:6379

# JWT
JWT_SECRET=
bBRf7kvmhwf9KALj1CrtQD42KbHBQhzB
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://web:3000,http://dashboard:3001

# Uploads
UPLOAD_BASE_URL=/uploads/products

# Sentry (opcjonalne)


# Printer Service
PRINTER_SERVICE_URL=http://printer-service:5000
